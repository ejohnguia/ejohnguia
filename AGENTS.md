# AGENTS.md

## Repo

This is a GitHub profile README repository (`ejohnguia/ejohnguia`). The only
tracked file is `README.md`, which renders on the GitHub profile. There is no
application code, build system, tests, or CI.

## Working here

- The only meaningful edit is `README.md`. No build, lint, test, or typecheck
  steps exist.
- Commit messages use conventional prefixes. The history uses `docs:`, `add:`,
  and `fix:`.
- Do not commit unless explicitly asked.
